## What comes next

Two next steps are clear from current state of code.

- full TOML config: shipped — `[[collections]]` array-of-tables syntax for inline collection declarations in vault.toml
- richer template slots: current slot model is strict and useful, but it is still shallow compared with a layout composition people expect from mature SSGs